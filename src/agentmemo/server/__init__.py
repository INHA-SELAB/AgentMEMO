"""HTTP MCP server (placeholder).

Goal: expose `MemoRepository` as MCP tools so multiple agents (Claude Code,
Codex, Gemini, …) can read and write memos concurrently against the same
SQLite store the GUI is reading.

This module is intentionally a stub for the UI-first MVP. The intended shape
of the public API is documented in `README.md` next to this file.
"""
